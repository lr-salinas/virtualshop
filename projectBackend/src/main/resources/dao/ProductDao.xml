<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="backend.project.dao.ProductDao">

    <select id="productdetails" resultType="backend.project.model.Product">
        SELECT product_id as productId,
               product_name as productName,
               details as details,
               size as size,
               stock as stock,
               color as color,
               quantity as quantity,
               cost_product as costProduct,
               status as status,
               create_date as createDate
        FROM product
        WHERE product_id =#{productId}
    </select>

    <select id="productlist" resultType="backend.project.model.Product">
        SELECT product_id as productId,
               product_name as productName,
               details as details,
               size as size,
               stock as stock,
               color as color,
               quantity as quantity,
               cost_product as costProduct,
               status as status,
               create_date as createDate
        FROM product
        WHERE status = 1
    </select>


</mapper>
